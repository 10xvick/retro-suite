const fs = require('fs');
const puppeteer = require('puppeteer');
(async () => {
    // Just a quick sanity check to see if setting ports 0 and 1 to AA/BB on reading 2140 helps
    console.log("No op");
})();
